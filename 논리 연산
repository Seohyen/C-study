true = 1
false = 0 
이 출력된다. 

&& AND 
AND연산자는 두 피연산자가 모두가 참일 때 true를 반환, 그 외의 경우는 false를 반환

|| OR  
OR연산자는 인수 중 하나라도 true이면 true를 반환, 그렇지 않으면 false를 반환

! NOT
NOT 연산자는 인수를 하나만 받고, 다음 순서대로 연산을 수행
1. 피연산자를 불린형(true / false)으로 변환,  변환된 값의 역을 반환

어디서 사용되는가? 
&& 두 개 이상의 경우를 충족한 값을 얻기 위해 
OR 연산자 ||은 if문에서 자주 사용됨 주어진 조건 중 하나라도 참인지를 테스트하는 용도
