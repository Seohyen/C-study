int 와 string을 더하고 싶을때 

int a = 10;
string b = "10"; 

Console.WhiteLine(a+b);
로 출력하면 1010이 출력된다. 

1. Parse함수 사용
Console.WhiteLine(a+int.Parse(b));
을 출력하면
20
이 출력된다. 
int f = int.Parse(b); 와 같이 사용할 수 있다. 

2. (자료형)a
float c = (float)a;
a를 float 형식으로 변환 할 수 있다. 
GetType으로 출력했을 때  c 가 single(float)형식으로 출력되는 것을 확인할 수 있다. 


3. Convert
int d = Convert.ToInt32(b);
int e = Convert.ToString(a);
와 같이 Convert를 이용하여 원하는 자료형으로 변환할 수 있다. 

4.ToString
출력할 때 a.ToString과 같이 출력하면 문자열로 출력되는 것을 확인할 수 있다. 

